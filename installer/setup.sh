# Git
pkg install git
git config --global credential.helper store
git config --global user.email "..."
git config --global user.name "..."

# Essentials
pkg install vim
pkg install nvim
pkg install htop
